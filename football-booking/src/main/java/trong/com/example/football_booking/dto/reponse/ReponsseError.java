package trong.com.example.football_booking.dto.reponse;

public class ReponsseError extends ResponseData {
    public ReponsseError(int status, String message) {
        super(status, message);
    }
}
